import { Router } from "express"

const userRouter:Router = Router()

//NOTA PARA NO OLVIDARME POR DONDE VOY. 
//vas a tener que hacer un archivo para cada serie de rutas (peticiones HTTP)
//despues llamarlas en el server y listo, intentaste hacerlo con un enrutador pero te estaba generando conflictos el orden de los archivos
